<?xml version="1.0" encoding="utf-8"?>
<!--
    Copyright (C) 2012 The Android Open Source Project
    modified
    SPDX-License-Identifier: Apache-2.0 AND GPL-3.0-only
-->

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/stopwatch_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:ignore="InconsistentLayout">

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/left_guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent=".1" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/right_guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent=".9" />

    <!-- Max height is set in the StopwatchFragement.java file -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/stopwatch_time_wrapper"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/laps_background"
        app:layout_constraintStart_toEndOf="@id/left_guideline"
        app:layout_constraintEnd_toStartOf="@id/right_guideline"
        app:layout_constraintDimensionRatio="1:1"
        tools:layout_constraintHeight_max="550dp">

        <!-- Sufficient space exists to include the bounding stopwatch circle. -->
        <include
            layout="@layout/stopwatch_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <com.best.deskclock.stopwatch.StopwatchCircleView
            android:id="@+id/stopwatch_circle"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Bottom margin, max height and padding are set programmatically in the
         StopwatchFragement.java file. -->
    <LinearLayout
        android:id="@+id/laps_background"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="10dp"
        android:layout_marginTop="10dp"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@id/stopwatch_time_wrapper"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:background="@drawable/bg_card_for_preview"
        tools:layout_marginBottom="10dp"
        tools:layout_constraintHeight_max="200dp">

        <include layout="@layout/lap_header" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/laps_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:visibility="visible"
            tools:listitem="@layout/lap_view" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
