<?xml version="1.0" encoding="utf-8"?>
<!--
    layout from https://github.com/ProjectInfinity-X/packages_apps_DeskClock
    modified
    SPDX-License-Identifier: Apache-2.0
-->

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/digital_widget"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/digitalWidgetBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        tools:src="@drawable/appwidget_digital_background" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <TextClock
            android:id="@+id/clock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:ellipsize="none"
            android:maxLines="1"
            android:textColor="@color/digital_widget_time_color"
            android:visibility="visible"
            tools:text="01:23 AM"
            tools:textSize="70sp" />

        <TextClock
            android:id="@+id/clockForCustomColor"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:ellipsize="none"
            android:maxLines="1"
            tools:visibility="gone" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:baselineAligned="false">

            <TextView
                android:id="@+id/date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="3dp"
                android:gravity="center"
                android:ellipsize="none"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:textColor="@color/widget_text_color"
                tools:text="Sat. 20 Jan."
                tools:textSize="18sp" />

            <TextView
                android:id="@+id/dateForCustomColor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="3dp"
                android:gravity="center"
                android:ellipsize="none"
                android:includeFontPadding="false"
                android:maxLines="1"
                tools:visibility="gone" />

            <!-- Do not use the App tint attribute, otherwise the icon will not
            change color when the device mode (light or dark) changes. -->
            <ImageView
                android:id="@+id/nextAlarmIcon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@null"
                android:scaleType="fitCenter"
                android:tint="@color/widget_text_color"
                tools:src="@drawable/ic_tab_alarm_static"
                tools:paddingHorizontal="3dp"
                tools:ignore="UseAppTint" />

            <ImageView
                android:id="@+id/nextAlarmIconForCustomColor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@null"
                android:scaleType="fitCenter"
                tools:visibility="gone" />

            <TextView
                android:id="@+id/nextAlarm"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="3dp"
                android:gravity="start|center_vertical"
                android:ellipsize="none"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:textColor="@color/widget_text_color"
                tools:text="Today 08:30 AM"
                tools:textSize="18sp" />

            <TextView
                android:id="@+id/nextAlarmForCustomColor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="3dp"
                android:gravity="start|center_vertical"
                android:ellipsize="none"
                android:includeFontPadding="false"
                android:maxLines="1"
                tools:visibility="gone" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:gravity="center"
            android:baselineAligned="false">

            <!-- Do not use the App tint attribute, otherwise the icon will not
                 change color when the device mode (light or dark) changes. -->
            <ImageView
                android:id="@+id/labelIcon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@null"
                android:scaleType="fitCenter"
                android:tint="@color/md_theme_widgetTertiary"
                tools:src="@drawable/ic_label"
                tools:paddingHorizontal="3dp"
                tools:ignore="UseAppTint" />

            <ImageView
                android:id="@+id/labelIconForCustomColor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@null"
                android:scaleType="fitCenter"
                tools:visibility="gone" />

            <TextView
                android:id="@+id/nextAlarmTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingStart="0dp"
                android:paddingEnd="3dp"
                android:gravity="center"
                android:ellipsize="end"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:maxEms="12"
                android:textColor="@color/md_theme_widgetTertiary"
                tools:text="Alarm title"
                tools:textSize="18sp" />

            <TextView
                android:id="@+id/nextAlarmTitleForCustomColor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingStart="0dp"
                android:paddingEnd="3dp"
                android:gravity="center"
                android:ellipsize="end"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:maxEms="12"
                tools:visibility="gone" />

        </LinearLayout>

        <ListView
            android:id="@+id/worldCityList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|top"
            android:divider="@null"
            android:visibility="gone" />

    </LinearLayout>

</RelativeLayout>
